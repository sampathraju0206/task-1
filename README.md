Clean and prepare a raw dataset (Mall Customer Segmentation Data) by handling missing values, duplicates, inconsistent formats, and incorrect data types.
## Tools Used
- Microsoft Excel
## Steps Performed
Identified missing values by using ctrl+g--->spsecial--->blanks---->ok.
Removed duplicate records using the "Remove Duplicates" feature.
no date columns.
Renamed column headers to lowercase and without spaces.
Checked and corrected data types (number, text, date).
 Saved the cleaned dataset as `Cleaned_Mall_Customers.xlsx`.
 The dataset is now properly cleaned and ready for data analysis.
 Next I repository it in github.
